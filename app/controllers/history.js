var args = arguments[0] || {};
$.history.open();
function viewResult(){
	//alert('clicked:'+Titanium.Platform.displayCaps.platformWidth);
 	var resultwin = Alloy.createController('history_item');
 }

